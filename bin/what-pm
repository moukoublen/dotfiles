#!/usr/bin/env bash

if command -v dnf &>/dev/null; then
  echo "dnf"
  exit 0
fi

if command -v apt &>/dev/null; then
  echo "apt"
  exit 0
fi

if command -v pkg &>/dev/null; then
  echo "pkg"
  exit 0
fi

echo ""
exit 1
