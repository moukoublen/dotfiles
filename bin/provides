#!/usr/bin/env bash

case "$(what-os)" in
  "Fedora"|"Fedora Linux")
    dnf provides "$@"
    ;;
  "Ubuntu"|"Debian"|"LinuxMint"|"Pop")
    dpkg -S "$@"
    ;;
  *)
    echo "this script does not have packages for this release"
    ;;
esac
